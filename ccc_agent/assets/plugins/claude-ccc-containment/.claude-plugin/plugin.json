{
  "name": "ccc",
  "version": "0.2.0",
  "description": "CCC contained-session lifecycle hooks and /ccc commands. Distributed through a read-only Claude plugin seed; hooks stay inert outside CCC sessions because they require CCC_AGENT_SESSION. Signals turn boundaries to the trusted supervisor over the control socket; it never holds commit authority.",
  "author": {
    "name": "CCC agent"
  }
}
